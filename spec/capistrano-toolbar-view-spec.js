'use babel';

import CapistranoToolbarView from '../lib/capistrano-toolbar-view';

describe('CapistranoToolbarView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
